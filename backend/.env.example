# Groq API キー（要約に使用。 https://console.groq.com/keys で発行）
GROQ_API_KEY=gsk_xxxxxxxxxxxxx

# サーバー設定
HOST=0.0.0.0
PORT=8000

# 本番のみ: フロントのURL（複数はカンマ区切り）。未設定なら *（開発用）
# CORS_ORIGINS=https://your-app.netlify.app
